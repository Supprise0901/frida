name: Build Android arm64 Frida CLI

on:
  workflow_dispatch:

jobs:
  build-frida-arm64:
    runs-on: ubuntu-22.04

    steps:
      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git \
            python3 python3-venv \
            ninja-build clang lld \
            gcc-aarch64-linux-gnu \
            pkg-config

      - name: Create Python venv and install Meson
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install 'meson>=1.1.0'
          meson --version

      - name: Clone frida with releng + meson submodules
        run: |
          git clone https://github.com/frida/frida.git
          cd frida
          git checkout 17.5.2
          git submodule update --init --recursive

      - name: Create Android arm64 cross-file
        run: |
          cd frida
          cat > android-aarch64.txt << 'EOF'
          [binaries]
          c = 'aarch64-linux-gnu-gcc'
          cpp = 'aarch64-linux-gnu-g++'
          ar = 'aarch64-linux-gnu-ar'
          strip = 'aarch64-linux-gnu-strip'
          pkg-config = 'pkg-config'

          [host_machine]
          system = 'android'
          cpu_family = 'aarch64'
          cpu = 'aarch64'
          endian = 'little'

          [properties]
          needs_exe_wrapper = true
          EOF

      - name: Configure Android arm64 build (explicit subsystem)
        run: |
          source venv/bin/activate
          cd frida
          meson setup build-android \
            --cross-file android-aarch64.txt \
            -Dsubsystem=android

      - name: Build frida CLI
        run: |
          source venv/bin/activate
          cd frida
          ninja -C build-android frida

      - name: Package frida CLI
        run: |
          cd frida
          mkdir -p out
          cp build-android/subprojects/frida-tools/frida out/frida
          file out/frida
          sha256sum out/frida

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: frida-17.5.2-android-arm64-cli
          path: frida/out/frida
